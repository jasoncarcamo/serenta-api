CREATE TABLE images (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    user_id INTEGER REFERENCES users(id) ON DELETE CASCADE NOT NULL,
    listings_id INTEGER REFERENCES listings(id) ON DELETE CASCADE NOT NULL
);